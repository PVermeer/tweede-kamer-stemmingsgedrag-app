import { DocumentLinkPipe } from './document-link.pipe';

describe('DocumentLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
